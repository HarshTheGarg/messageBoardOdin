extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/new") New Message 
  ul
    each message in messages
      li 
        ul
          each val, key in message
            li= key + ": " + val
          br
